# Planted blockers

Phase 3 of the course build opens three planted PRs against this repo's
`main` branch (see SETUP.md §7.3 for the full spec of each). This directory
is a pointer only — the PRs themselves live on their own branches, not here.
